Question: {question}

Current Memory (Already Known/Verified):
{memory}

## Task:
Based on the Memory above, identify ALL independent, non-redundant pieces of information needed to answer the question.

## Rules:
1. DO NOT repeat information already in Memory
2. Each plan should verify a DIFFERENT entity or relationship
3. DO NOT generate multiple plans about the same thing (e.g., don't generate both "X is Y's son" and "Y is X's mother")
4. Use specific names from Memory in your plans
5. Each plan should be independently verifiable

Generate all necessary plans:
