二、推荐的 IVF 索引方案（结论先行）
✅ 强烈推荐方案（平衡成功率 / 精度 / 工程成本）
索引类型：IVF4096,Flat
向量精度：float32
构建方式：CPU FAISS
查询方式：CPU + mmap


这是你当前配置下的“甜点方案”。

三、为什么选 IVF4096,Flat（而不是别的）
1️⃣ nlist = 4096 是“规模最优点”

计算一下倒排表大小：

21,015,324 / 4096 ≈ 5,130 vectors / list


这是一个非常理想的范围：

1k：list 不会太稀疏

< 10k：list 内搜索很快

训练 k-means 仍然可控

2️⃣ 为什么不用更小 / 更大？
nlist	问题
1024	list 太大，nprobe 得调很高，召回不稳
2048	勉强可用，但不如 4096
4096	✅ 性价比最优
8192	训练慢，收益递减
四、完整资源占用评估（重点）

下面是构建阶段和使用阶段分开算。

一）索引构建阶段（最吃资源）
1️⃣ 向量本体（必须）
21M × 768 × 4B ≈ 64 GB

2️⃣ IVF 聚类训练（k-means）

FAISS 的 IVF 训练不会用全量数据，默认会采样：

典型：min(256k, N) vectors

内存占用：≈ 256k × 768 × 4B ≈ 0.75 GB

👉 几乎可以忽略

3️⃣ 向量 add 到 IVF 索引时的 buffer

FAISS 在 add() 时：

需要一块连续 buffer

通常 ≈ 1× 向量大小

≈ 64 GB

4️⃣ Python / PyTorch / 数据管道开销

经验值（你已经验证过）：

≈ 8–12 GB

🔥 构建阶段内存峰值（关键结论）
64  (向量)
+64 (FAISS add buffer)
+10 (其他)
≈ 138 GB   ← 理论极限

⚠️ 为什么你还能跑？

因为 IVF 不会同时把所有 buffer 堆满：

实际峰值通常是 Flat 的 60–70%

经验峰值：

≈ 70–85 GB


👉 完全落在你“100多G可用内存”的安全区间内

二）构建时间估算
1️⃣ 编码（你已经测过）

双 GPU

batch_size ≈ 2048

≈ 8–9 小时

2️⃣ IVF 训练

256k 采样

k-means (4096 centers)

≈ 5–10 分钟

3️⃣ 向索引 add 全量向量

这是最慢的一步（CPU-bound）：

≈ 1.5 – 2.5 小时

⏱️ 总构建时间
≈ 10 – 12 小时

三）最终索引文件大小（磁盘）
项	大小
IVF4096,Flat index	≈ 45–50 GB
元数据 / ids	≈ 2–4 GB
总计	≈ 50–55 GB

👉 SSD 必须够快（NVMe 最好）

五、查询阶段资源占用（你真正跑实验时）
推荐查询参数
index.nprobe = 16   # 起步
# 或 32（如果你要 Top-100 recall）

查询阶段内存（mmap）

如果你用：

faiss.read_index(path, faiss.IO_FLAG_MMAP)

常驻内存：
≈ 25–35 GB

剩余走 OS page cache（按需）

👉 非常适合你这种“内存有限但磁盘够快”的机器

查询延迟（经验值）

Top-100

nprobe = 16

CPU 单线程：

≈ 5–15 ms / query


多线程可线性加速。