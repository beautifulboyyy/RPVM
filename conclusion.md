## 维基百科索引构建实验总结

### 实验目标
在服务器上为2100万条维基百科文档构建E5-base-v2检索索引，用于RPVM多跳问答实验。

---

### 实验记录

#### 实验1：Flat索引 + 保存embedding
**配置**：
- 索引类型：`Flat`
- batch_size：512
- 参数：`--save_embedding`

**结果**：❌ 失败
- 编码完成（100%，耗时9.67小时）
- **失败原因**：保存embedding阶段OOM
- 内存峰值：109GB（超出系统125GB限制）

#### 实验2：Flat索引 + 不保存embedding
**配置**：
- 索引类型：`Flat`
- batch_size：2048（优化）
- GPU：双卡（GPU 1+2）
- 去掉 `--save_embedding`

**结果**：❌ 失败
- 编码完成（100%，耗时8.5小时）
- **失败原因**：构建FAISS索引阶段OOM
- 内存峰值：114GB

---

### 失败根本原因

**Flat索引的内存瓶颈**：

```
FAISS Flat索引 = 完整向量存储
索引大小 = 21,015,324文档 × 768维 × 4字节 ≈ 64GB

构建阶段内存需求：
- 语料库：~20GB
- 编码缓冲：~10GB
- FAISS索引：~64GB
- 序列化开销：~20GB
总计：~114GB > 可用内存(46GB)
```


---

### 改进方向

#### 方向1：使用IVF索引（推荐）⭐
```bash
--faiss_type IVF4096,Flat
```

**优势**：
- 内存占用：降低30-40%（~70-80GB峰值）
- 索引文件：减小20-30%（~45-50GB）
- 检索速度：更快
- 精度损失：极小（<1%）

#### 方向2：使用压缩索引
```bash
--faiss_type IVF4096,PQ64
```

**优势**：
- 内存占用：大幅降低（~15-20GB峰值）
- 索引文件：极小（~5-8GB）
- **权衡**：检索精度降低5-10%

#### 方向3：硬件升级
- 增加服务器内存至256GB
- 或使用更大内存的节点

#### 方向4：分片构建（需开发）
- 将语料库分成4-8份
- 分别构建索引后合并
- 需要自定义合并脚本

---

### 推荐方案

**立即可行**：使用 `IVF4096,Flat`
- 预计成功率：95%
- 内存峰值：~75GB
- 耗时：~9小时

**最优方案**：先用IVF索引完成实验，后续有更多资源时再构建Flat索引作为对比基准。